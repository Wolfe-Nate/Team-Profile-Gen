//!html append in here for cards
//for loop for manager etc

const generateHTML = (data) => {
  return `
    <!DOCTYPE html>

    <main>
        ${functionthatcreatescardsusingdata}
    </main>
    `;
};

module.exports = generateHTML;
